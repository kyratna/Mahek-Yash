// Minimal hash-based "routing" — just enough to give the Blessings Wall its
// own page/URL without pulling in a router library. The site has exactly one
// extra route, so a plain hash check in main.jsx is simpler than a real router.
export const BLESSINGS_WALL_HASH = "#/blessings-wall";
export const HOME_HASH = "";
